import java.io.IOException;

public class PrintSomeSymbol {
    public static void main(String[] args) throws InterruptedException {
        int numThreads = 3;
        int controlValue = 0;
        /*
        SyncInt permission = new SyncInt();
        Thread[] first = new Thread(new SomeSymbolSynchTest('|', permission, numThreads, controlValue));
        Thread[] second = new Thread(new SomeSymbolSynchTest('\', permission, numThreads, controlValue));
        Thread[] third = new Thread(new SomeSymbolSynchTest('/', permission, numThreads, controlValue));

        first.start();
        second.start();
        third.start();

         */
    }
}
